# Лабораторна робота 3.2 — варіант 8

## Що реалізовано

Усі пункти 1–4 з методички:

1. Клас `MyString` за варіантом 8:
   - `Value` — значення рядка;
   - `Length` — довжина;
   - пошук заданого символу;
   - розворот рядка;
   - додавання нового рядка;
   - виведення рядка.

2. Створено 4 об'єкти та показано роботу з:
   - масивом `MyString[]`;
   - non-generic колекцією `ArrayList`;
   - Generic-колекцією `List<MyString>`.

   Для кожного варіанта показано:
   - додавання;
   - видалення;
   - оновлення;
   - пошук;
   - прохід по елементах.

3. Реалізовано узагальнене бінарне дерево `BinaryTree<T>`.

4. Реалізовано:
   - `IComparable<MyString>`;
   - сортування через `List.Sort()`;
   - власний ітератор `PostOrderEnumerator<T>` через `IEnumerator<T>`;
   - `IEnumerable<T>` у дереві;
   - обхід дерева за варіантом 8: **postorder**.

## Структура проєкту

```text
Lab32_Variant8/
├── Lab32_Variant8.csproj
├── Program.cs
├── MyString.cs
├── BinaryTree.cs
├── PostOrderEnumerator.cs
└── README.md
```

## Як запустити у VS Code на Mac

Відкрий Terminal у папці проєкту та виконай:

```bash
dotnet --version
dotnet build
dotnet run
```

Якщо VS Code не бачить C#-проєкт, відкрий саме всю папку `Lab32_Variant8` через **File → Open Folder**.

## Що показувати на захисті

1. `MyString.cs` — дані та 4 методи твого варіанта.
2. `Program.cs` — 4 об'єкти.
3. У `Program.cs` — масив, `ArrayList`, `List<MyString>` та всі операції над ними.
4. `CompareTo()` у `MyString.cs` — порівняння за довжиною.
5. `list.Sort()` у `Program.cs` — демонстрація сортування.
6. `BinaryTree.cs` — узагальнений тип `BinaryTree<T>`.
7. `PostOrderEnumerator.cs` — власний ітератор.
8. У `FillPostOrder()` пояснити порядок: **Left → Right → Root**.
9. Виконати `dotnet run` і показати результат.

## Короткі пояснення

### Масив
Розмір фіксований. Для додавання або видалення створюється новий масив.

### ArrayList
Розмір змінюється, але це non-generic колекція: елементи зберігаються як `object`.

### List<T>
Generic-колекція. Розмір змінюється і тип елементів контролюється компілятором.

### IComparable<T>
Дозволяє класу самому описати, як порівнювати два об'єкти. Тут рядки порівнюються за довжиною.

### IEnumerable<T> та IEnumerator<T>
`IEnumerable<T>` дозволяє отримати ітератор, а `IEnumerator<T>` виконує сам прохід по елементах. Завдяки цьому дерево можна обходити через `foreach`.

### Postorder
Для варіанта 8 порядок такий:

```text
ліве піддерево → праве піддерево → корінь
```
